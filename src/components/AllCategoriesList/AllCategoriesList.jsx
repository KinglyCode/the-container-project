import FeaturedItemList from "../FeaturedItemsList/FeaturedItemList";

export default function AllCategoriesList() {
    return (
        <FeaturedItemList />
    )

    // const allItems = items.map(item => {
    //     return(
    //         <AllCategories
    //           key={item._id}
    //           item={item}
    //           handleAddToCart={handleAddToCart}
    //           />
    //     )})
    
    //     return (
    //         <main>
    //             {allItems}
    //         </main>
    //     )
}
